## 2025-05-22 - [Enhanced Character Counter UX]
**Learning:** Shared components like `EnhancedTextarea` often have placeholder logic for counters. Implementing a robust counter requires `tabular-nums` to prevent layout shift and `aria-live` to announce proximity to limits.
**Action:** Always check shared UI components for "placeholder" implementations that can be polished into full features.
