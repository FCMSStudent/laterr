# SQLite Local Database Configuration
# No external services required - all data is stored locally in the browser

# Optional: OpenAI API Key (for embeddings generation - currently not implemented in local mode)
# Get this from: https://platform.openai.com/api-keys
# Note: Embeddings and AI features are currently disabled in local SQLite mode
# LOVABLE_API_KEY=your_openai_api_key

# Optional: Development/Testing
# VITE_DEV_MODE=true
