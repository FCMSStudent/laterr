-- Suggested index for performance optimization
-- File: src/shared/hooks/useDashboardStats.ts
-- Context: Found filter on "measured_at" in table "health_measurements"
CREATE INDEX IF NOT EXISTS idx_health_measurements_measured_at ON health_measurements (measured_at);

-- Suggested index for performance optimization
-- File: src/shared/hooks/useDashboardStats.ts
-- Context: Found filter on "measured_at" in table "health_measurements"
CREATE INDEX IF NOT EXISTS idx_health_measurements_measured_at ON health_measurements (measured_at);

-- Suggested index for performance optimization
-- File: src/shared/hooks/useDashboardStats.ts
-- Context: Found filter on "measured_at" in table "health_measurements"
CREATE INDEX IF NOT EXISTS idx_health_measurements_measured_at ON health_measurements (measured_at);

-- Suggested index for performance optimization
-- File: src/shared/hooks/useDashboardStats.ts
-- Context: Found filter on "measured_at" in table "health_measurements"
CREATE INDEX IF NOT EXISTS idx_health_measurements_measured_at ON health_measurements (measured_at);
